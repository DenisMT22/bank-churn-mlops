# Dependances optionnelles pour un deploiement Google Cloud.
# Le projet fonctionne entierement sans elles : l'import de google.cloud
# dans src/models/retrain.py est protege par un try/except et l'envoi
# vers Cloud Storage est simplement ignore si le paquet est absent.

google-cloud-storage==2.10.0
